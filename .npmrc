# npm 7+ enforces peer deps strictly. @copilotkit has a peer conflict on
# @copilotkit/runtime-client-gql that breaks `npm install` on clean installs
# (Railway/Vercel). legacy-peer-deps matches npm 6 behavior so deploys resolve.
legacy-peer-deps=true
