/**
 * Scanner layout — standalone, no agent system, no Renata sidebar.
 * Updated: chart height 580
 */
export default function ScannerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
